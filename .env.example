# Server Configuration
PORT=3000
NODE_ENV=development

# MongoDB Connection
# Local MongoDB:
MONGODB_URI=mongodb://localhost:27017/marketing-tasks

# MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/marketing-tasks

# CORS (leave empty for all origins)
CORS_ORIGIN=
